# .readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    nodejs: "20"
  jobs:
    pre_install:
      - npm ci --include=dev

python:
  install:
    - requirements: requirements.txt

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: false
